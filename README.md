# assignment-2

## Part 2 Integration Testing

Run the StreetRace Manager demo CLI from the repository root:

```bash
python3 -m integration.code.cli
```

Run the integration test suite from the repository root:

```bash
python3 -m pytest integration/tests -v
```
